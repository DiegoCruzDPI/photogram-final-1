<h1><%= @user.username %></h1>

<dt> Private </dt>
<dd>Ask for help this is not hard but sticky</dd>

<dt>Followers</dt>
<dd><%= @user.followers.count %></dd>
<dd><button>Follow Not working</button></dd>

<dt>Following</dt>
<dd><%= @user.leaders.count %></dd>

<div>
  <nav>
  <ul>
    <li>
      <a href ="/users/<%= @user.username %>">Profile (Make the links work)</a>
    </li>
    <li>
      <a href ="">Liked Photos</a>
    </li>
    <li>
      <a href ="">Feed</a>
    </li>
    <li>
      <a href ="">Discover</a>
    </li>
    </ul>
  </nav>

  <div>
  <div>
    <table border="1">
      <tr>

        <th>
          Image
        </th>

        <th>
          Caption
        </th>

        <th>
          Owner
        </th>

        <th>
          Posted
        </th>

        <th>
          Likes 
        </th>

        <th>
        </th>
      </tr>

      <h2>Own photos (<%= @user.own_photos.count %>)</h2>

      <% @user.own_photos.each do |a_photo| %>
      <tr>

        <td>
          <%= a_photo.image %>
        </td>

        <td>
          <%= a_photo.caption %>
        </td>

        <td>
          <%= a_photo.owner_id %>
        </td>

        <td>
          <%= time_ago_in_words(a_photo.created_at) %> ago
        </td>

        <td>
          <%= a_photo.likes_count %>
        </td>

        <td>
          <a href="/photos/<%= a_photo.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>


</div>
